version: "3.9"

services:
  predictive-deployer:
    image: docker:27-cli
    container_name: predictive-deployer
    privileged: true
    working_dir: /workspace
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./:/workspace
    entrypoint: ["/bin/sh", "/workspace/deploy.sh"]
    restart: "no"